# 进程的多线程监控设计方案
## 0. 概述

本方案旨在设计和实现一个跨平台的进程与线程 CPU 使用情况监控系统，能够在多种硬件平台（如 Ubuntu x86 和 RK3566）上高效地监测并记录系统资源的使用情况。该系统不仅实现了对进程和线程的 CPU 使用数据采集，还包括数据处理、存储、分析与可视化的全过程。整个方案的设计充分考虑了不同硬件架构的差异性，确保其在多种平台上的兼容性与高效性。同时，方案提供了可扩展的模块化设计，支持通过静态库或动态库的形式与其他 C 或 C++ 项目集成，支持对多个进程的同时监控。

项目源码托管在公司内部 Git 仓库，可通过以下链接访问：  
**代码位置：** https://git.zh.midea.com/liudg16/thread-monitor

## 1. 数据采集与处理（C++部分）

### 1.1 基于 Linux `/proc` 文件系统的监控机制

`/proc` 文件系统是 Linux 提供的一种虚拟文件系统，主要用于获取操作系统核心数据以及进程和线程的状态信息。本方案充分利用了这一特性，通过解析 `/proc` 下的相关文件，精确获取目标进程和线程的各种运行状态，包括但不限于 PID、线程 ID、状态、用户态时间 (utime) 和内核态时间 (stime) 等关键信息。

- **跨平台数据获取：** 
  - 在 Ubuntu x86 平台上，通过读取 `/proc/[pid]/stat` 和 `/proc/[pid]/task/[tid]/stat` 文件，获取进程和线程的实时状态信息。例如，`cat /proc/27562/stat` 获取进程信息，`cat /proc/27562/task/27563/stat` 获取线程信息。
  - 在 RK3566 平台上，由于内核配置的不同，需使用类似的命令如 `cat /proc/1073/stat` 和 `cat /proc/1073/task/1081/stat` 获取相应的信息。

### 1.2 基于 C++ 定时器和循环机制的数据采集程序

为了确保数据采集的实时性和稳定性，开发了一个高效的 C++ 程序，利用定时器或循环机制定期读取 `/proc` 文件系统中的数据。该程序实现过程中考虑了跨平台兼容性，尤其是不同平台上 `stat` 文件中字段的位置差异。例如：

- 在 RK3566 上，进程优先级和 `nice` 值位于 `stat` 文件的第 19 和 20 个字段；
- 在 Ubuntu x86 上，相应的字段位置分别为第 18 和 19 个字段。

这种架构特异性的处理确保了程序能够在不同平台上正确解析和处理进程与线程的相关数据。

## 2. 数据处理与存储

### 2.1  数据解析与处理

数据处理是本方案的核心环节之一，主要通过解析 `/proc/[pid]/task/[tid]/stat` 文件，提取并计算各个线程的 CPU 使用情况。

- **ParseStatFile 函数：** 
  - 该函数负责解析 `stat` 文件，从中提取线程的用户态时间 (`utime`) 和内核态时间 (`stime`) 以便后续计算。
  
- **PrintProcessInfo 函数：** 
  - 该函数提取并打印线程的优先级、`nice` 值等信息，既可以输出到控制台，也可以记录到日志文件中，确保用户和系统日志可以准确追踪到每个线程的关键性能指标。

- **ComputeCpuUsage 函数：** 
  - 通过比较当前采集周期与前一周期的 CPU 使用情况，计算每个线程在该时间段内的 CPU 使用百分比。此计算不仅考虑了用户态与内核态的分离，还综合考虑了总 CPU 时间的变化，确保 CPU 使用率的准确性。

### 2.2 数据存储

- **紧凑型日志记录：**
  - 由于嵌入式设备往往具有较为有限的存储资源，方案设计时特别注重数据存储的紧凑性。处理后的 CPU 使用率数据被记录为精简的文本格式，以节省存储空间。
  
- **日志文件轮转：**
  - 当日志文件达到预设的大小限制（如 10MB）时，程序会自动触发日志轮转。`RotateLogFileIfNeeded` 函数会创建新的日志文件，并将基本信息重新写入新文件的开头，保证数据的连贯性与完整性。


## 3. Python 数据解析与可视化

### 3.1 数据聚合与统计分析

为了便于后续的分析和可视化展示，设计了 Python 脚本用于处理和分析采集到的日志数据。

- **线程数据聚合：**
  - 在实际应用中，同一进程内可能存在多个名称相同的线程。为了避免统计数据的重复计算，脚本会在处理阶段将这些线程的 CPU 使用量合并。
  - 使用 Python 的字典结构 `temp_data` 来存储和聚合同名线程的 CPU 使用数据。

- **统计信息计算与展示：**
  - `print_statistics` 函数负责计算并展示各线程的最大、平均用户态和内核态 CPU 使用率，为用户提供直观的性能概览。
  
- **CPU 使用率曲线绘制：**
  - `plot_cpu_usage` 函数用于生成进程和线程的 CPU 使用率随时间变化的曲线图。为了突出整体进程的 CPU 使用情况，特意将其曲线用黑色加粗显示，而线程的曲线则以不同的颜色和样式显示，便于区分和对比。


### 3.2 进程总 CPU 使用情况计算

- **calculate_process_cpu 函数：**
  - 该函数通过汇总所有线程的 CPU 使用情况，计算出整个进程的总 CPU 使用率。这一数据不仅反映了单个线程的负载情况，更重要的是提供了进程级别的整体资源占用情况，有助于系统性能的全面评估。

- **绘图优先级：**
  - 在生成图表时，系统优先绘制整个进程的 CPU 使用曲线，并将其置于最上方显示，确保用户能够一目了然地看到进程的总 CPU 使用情况。



## 4. 动态库和静态库支持

为了增强该系统的模块化和可复用性，本方案提供了动态库和静态库的支持，允许其他 C 或 C++ 项目通过库函数接口调用监控功能。通过将主要功能封装为库，用户可以将本方案轻松集成到现有系统中，从而在复杂项目中实现灵活的进程和线程监控。

- **库的编译：** 
  - 动态库可以通过 `g++ -fPIC -shared cpu_monitor.cpp -o libcpumonitor.so` 进行编译。
  - 静态库可以通过 `ar rcs libcpumonitor.a cpu_monitor.o` 进行创建。

- **C 接口支持：** 
  - 提供了 C 语言兼容的接口函数，使得非 C++ 项目也可以轻松调用这些监控功能。

- **C 语言接口函数：**
  - `void monitor_processes(const char* process_names[], int process_count, int refresh_delay);`
  - `void monitor_processes_by_pids(const int pids[], int pid_count, int refresh_delay);`

这些接口使得用户能够通过传入进程名列表或 PID 列表来监控多个进程，每个进程都会生成独立的日志文件。

## 5. 多个进程同时分析支持

本方案支持同时监控多个进程，无论是通过进程名还是 PID，系统都会为每个进程生成单独的日志文件。在监控过程中，每个进程的 CPU 使用情况会分别记录到独立的 CSV 文件中。

- **输入格式：** 
  - 用户可以通过命令行输入多个进程名或 PID，使用空格分隔。例如：
    ```bash
    ./your_program -n 2 process1 process2 1234
    ```
    其中 `process1`、`process2` 为进程名，`1234` 为 PID。
  
- **日志文件生成：** 
  - 每个进程会生成一个以进程名或 PID 命名的日志文件（如 `process1.csv`、`process_1234.csv`）。

## 6. 执行结果示例

以下是 Python

 解析脚本执行后的示例结果，展示了通过分析日志数据获得的 CPU 使用情况：

```bash
$ sudo python3 cpu_read.py /media/sf_shared/process_1040.csv
Process ID: 1040 | Prio: 0
planner_node: Prio=0 | Max/Avg User=8.0%/0.57% | Max/Avg Kernel=1.0%/0.83%
MotionManager: Prio=0 | Max/Avg User=2.0%/1.10% | Max/Avg Kernel=0.0%/0.00%
SensorPreproces: Prio=0 | Max/Avg User=11.0%/5.51% | Max/Avg Kernel=0.0%/0.00%
LineLaserLocalM: Prio=0 | Max/Avg User=1.0%/1.00% | Max/Avg Kernel=0.0%/0.00%
PlannerStub: Prio=0 | Max/Avg User=1.0%/1.00% | Max/Avg Kernel=0.0%/0.00%
IPC-SUBSCRIBES: Prio=0 | Max/Avg User=1.0%/1.00% | Max/Avg Kernel=0.0%/0.00%
Core_poseThread: Prio=0 | Max/Avg User=1.0%/1.00% | Max/Avg Kernel=0.0%/0.00%
```
